### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb')
class CardGame


  def checkforAce(card)
    if card.value = 1
      # should be == instead of =
      return true
    else
      return false
    end
  end

  dif highest_card(card1 card2)
  # spelling - should be def instead of dif
  # also missing , between two arguments
  if card1.value > card2.value
    return card.name # name is not a valid value
  else
    card2
  end
end
end # remove this end

def self.cards_total(cards)
  total # should be set to =0
  for card in cards
    total += card.value
    return "You have a total of" + total # return needs to be out of loop
    #should use string interpolation at total
  end
end

# end required here
```
